# استقرار روی سی‌پنل و رفع ۴۰۳

سایت استاتیک است و هیچ نیازی به PHP، دیتابیس یا Node روی هاست ندارد. فقط
فایل‌ها باید در جای درست و با دسترسی درست بنشینند.

## چه چیزی آپلود شود

**همه‌چیز به‌جز این‌ها:**

```
.git/            تاریخچهٔ گیت
node_modules/    اگر وجود دارد
scripts/         ابزار build و audit — سایت به آن‌ها نیاز ندارد
```

`scripts/` و پوشه‌های `_source/` اگر هم آپلود شوند، `.htaccess` دسترسی وب به
آن‌ها را می‌بندد. ولی آپلود نکردنشان ساده‌تر است.

**ساختار درست در `public_html`:**

```
public_html/
    index.html          ← نه public_html/carbonix/index.html
    .htaccess
    404.html
    robots.txt
    sitemap.xml
    fa/  en/  ar/
    assets/
```

## دسترسی‌ها

پوشه‌ها `755`، فایل‌ها `644`. در File Manager سی‌پنل: انتخاب همه ←
Permissions. یا در Terminal اگر هاست فعالش کرده:

```bash
cd ~/public_html && find . -type d -exec chmod 755 {} \; && find . -type f -exec chmod 644 {} \;
```

**نکتهٔ مهم:** خودِ `public_html` هم باید `755` باشد، و پوشهٔ خانه `711` یا
`755`. اگر فایل‌های داخل درست باشند ولی پوشهٔ والد اجازهٔ ورود ندهد، سرور
۴۰۳ می‌دهد.

---

# ۴۰۳ Forbidden — عیب‌یابی به ترتیب احتمال

متن «Access to this resource on the server is denied!» صفحهٔ پیش‌فرض
**LiteSpeed** است، که روی بیشتر سی‌پنل‌ها اجرا می‌شود.

فایل `.htaccess` این پروژه **هیچ قانون منع‌کننده‌ای برای صفحات سایت ندارد** —
نه `Deny`، نه `Require all denied` روی محتوا، نه `Options -Indexes`. پس ۴۰۳
تقریباً همیشه از حساب هاست می‌آید، نه از این فایل. با این حال ترتیب زیر
قطعی مشخص می‌کند کدام است.

## ۱. لاگ خطا را ببینید — این علت را با اسم می‌گوید

سی‌پنل ← **Metrics** ← **Errors**

یا در File Manager فایل `~/logs/error_log` را باز کنید. آخرین چند خط دقیقاً
می‌گوید چرا رد شده. اگر این را دارید، بقیهٔ مراحل لازم نیست.

پیام‌های رایج و معنایشان:

| در لاگ | یعنی |
|---|---|
| `Directory index forbidden by Options directive` | `index.html` در آن مسیر پیدا نشد — مرحلهٔ ۲ |
| `client denied by server configuration` | دسترسی یا مالکیت — مرحلهٔ ۳ |
| `ModSecurity: Access denied` | مرحلهٔ ۵ |
| `.htaccess: <directive> not allowed here` | مرحلهٔ ۴ |

## ۲. جای آپلود و ریشهٔ داکیومنت

شایع‌ترین علت، و دقیقاً ۴۰۳ می‌دهد نه ۴۰۴: محتوا داخل زیرپوشه است ولی
`public_html` خودش `index.html` ندارد. listing خاموش است، پس سرور به‌جای
نمایش فهرست، درخواست را رد می‌کند.

- File Manager را باز کنید و ببینید `index.html` **مستقیماً** داخل
  `public_html` هست یا داخل یک پوشهٔ دیگر.
- اگر دامنهٔ فرعی یا Addon Domain است: سی‌پنل ← **Domains** ← ستون
  Document Root را ببینید. ممکن است به پوشهٔ دیگری اشاره کند.

**تست ۳۰ ثانیه‌ای:** در File Manager داخل `public_html` پوشهٔ `test` بسازید،
داخلش فایل `index.html` با محتوای `ok` بگذارید، و `دامنه.com/test/` را باز
کنید.

- `ok` دیدید → ریشه و دسترسی‌ها سالم‌اند، برو مرحلهٔ ۴
- باز ۴۰۳ → مشکل حساب یا ریشهٔ داکیومنت است، مرحلهٔ ۳

## ۳. مالکیت فایل‌ها

در File Manager ستون Owner را نگاه کنید. باید **یوزر سی‌پنل شما** باشد. اگر
`root` یا `nobody` است، سرور رد می‌کند و از پنل قابل اصلاح نیست:

- فایل‌ها را پاک کنید و **از داخل File Manager** دوباره آپلود/استخراج کنید
  (نه از طریق ابزار بیرونی که با یوزر دیگری می‌نویسد)
- یا از پشتیبانی هاست بخواهید `chown` کند

## ۴. آیا `.htaccess` مقصر است — تست قطعی

در File Manager:

1. `.htaccess` را به `htaccess.bak` تغییر نام دهید
2. سایت را باز کنید

| نتیجه | معنی |
|---|---|
| باز ۴۰۳ | `.htaccess` بی‌گناه است. برگردانیدش و به مرحلهٔ ۵ بروید |
| سایت آمد | یک directive را هاست شما اجازه نمی‌دهد |

اگر سایت آمد: محتوای `scripts/htaccess-minimal.txt` را به‌عنوان `.htaccess`
بگذارید. آن نسخه فقط شامل چیزهایی است که سایت بدونشان کار نمی‌کند و هیچ
قانون منع‌کننده‌ای ندارد.

- با نسخهٔ حداقلی کار کرد → مقصر یکی از بلوک‌های `Header`، `Expires`،
  `Deflate` یا `Rewrite` است. بلوک‌ها را یکی‌یکی از فایل اصلی برگردانید تا
  مقصر پیدا شود.
- با نسخهٔ حداقلی هم ۴۰۳ → `AllowOverride` روی هاست شما اجازهٔ `.htaccess`
  نمی‌دهد؛ از پشتیبانی بخواهید فعالش کنند.

## ۵. ModSecurity و ابزارهای مسدودکنندهٔ سی‌پنل

این‌ها همه ۴۰۳ می‌دهند و هیچ‌کدام در `.htaccess` دیده نمی‌شوند:

| در سی‌پنل | چه می‌کند |
|---|---|
| **Security** ← ModSecurity | اگر برای دامنه روشن است، موقتاً خاموش کنید و تست کنید |
| **Security** ← IP Blocker | ببینید IP خودتان بلاک نشده |
| **Security** ← Hotlink Protection | اگر روشن است ۴۰۳ می‌دهد؛ خاموش کنید |
| **Security** ← Leech Protection | روی پوشهٔ سایت نباشد |

اگر هیچ‌کدام از این ابزارها در پنل شما نیست، از پشتیبانی بخواهید لاگ
ModSecurity دامنه‌تان را ببینند — آن لاگ در `error_log` شما نمی‌آید.

---

# بعد از اینکه سایت باز شد

`دامنه.com/check.html` را باز کنید. آن صفحه به CSS و JS سایت وابسته نیست، پس
حتی وقتی همه‌چیز خراب است هم رندر می‌شود، و می‌گوید کدام فایل نرسیده و با چه
`Content-Type`.

دو چیز را مخصوصاً چک کنید، چون تازه روی Apache واقعی اجرا می‌شوند:

1. **`Content-Type` تصاویر** — `.avif` باید `image/avif` باشد نه
   `application/octet-stream`. اگر نبود، بلوک `mod_mime` در `.htaccess` اجرا
   نشده.
2. **ریدایرکت‌ها** — `دامنه.com/fa/systems/` باید به `/fa/products/` برود، و
   `/fa/products/cold-brew-kettles/` هم به `/fa/products/`.
